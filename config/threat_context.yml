# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
generated_at: "2026-04-25T13:00:00Z"
expires_at: "2026-04-28T13:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - cve_id: "CVE-2026-32201"
    package: "n/a — SharePoint (not in our stack)"
    affected_versions: "SharePoint Server"
    severity: "critical"
    status: "actively_exploited"
    summary: "SharePoint spoofing zero-day actively exploited in the wild. Not in our stack but signals active Patch Tuesday exploitation."
    source_url: "https://msrc.microsoft.com"
    our_version: "n/a"
    action: "No action required — not in our dependency tree. Monitor for Python ecosystem spillover."

# No CVEs currently affect our pinned dependencies:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0
# All versions current as of 2026-04-25.

trending_techniques:
  - technique: "Supply chain compromise via package registries (npm self-propagating worm)"
    mitre_id: "T1195.002"
    relevance: "medium"
    context: "Bitwarden CLI Shai-Hulud worm propagated through npm by injecting malicious code into victims' own packages. PyPI equivalent attacks are plausible — our requirements.txt pins with == but lacks integrity hashes."
  - technique: "Firmware-persistent backdoors (FIRESTARTER on Cisco ASA/FTD)"
    mitre_id: "T1542.004"
    relevance: "low"
    context: "Custom ELF backdoor survives firmware updates on Cisco devices. Novel persistence mechanism but targets network appliances, not web application stacks like ours."
  - technique: "Vishing for SSO credential theft (ADT/Okta breach)"
    mitre_id: "T1566.004"
    relevance: "low"
    context: "ShinyHunters compromised ADT via vishing attack on employee Okta SSO. Relevant pattern for any org using SSO — not directly applicable to Threat-Feed's architecture but worth noting for Railway/GitHub account security."
  - technique: "Path traversal and privilege escalation on management consoles"
    mitre_id: "T1068"
    relevance: "medium"
    context: "CISA KEV additions for Samsung MagicINFO (path traversal) and SimpleHelp (privilege escalation). Pattern: management/admin consoles remain high-value targets. Our /api/ endpoints should continue to enforce rate limiting and input validation."

active_campaigns:
  - name: "FIRESTARTER — Cisco ASA/FTD firmware backdoor"
    actor: "Unknown (nation-state assessed per CISA/NCSC)"
    targets: "U.S. federal agencies, critical infrastructure"
    relevance_to_us: "low"
    summary: "CISA ED 25-03 mandates patching and physical power cycles by April 30. Affects Cisco ASA/FTD, not our stack. Significant for the threat landscape — firmware persistence is escalating."
  - name: "Shai-Hulud — npm supply chain worm"
    actor: "Unknown"
    targets: "JavaScript/npm ecosystem, downstream package consumers"
    relevance_to_us: "medium"
    summary: "Self-propagating worm from Bitwarden CLI compromise. 90-minute exposure window but downstream cascade still being assessed. We use Python/PyPI not npm, but supply chain worm techniques are cross-ecosystem."
  - name: "ShinyHunters — ADT data theft via vishing"
    actor: "ShinyHunters"
    targets: "ADT (10M+ customer records), Okta SSO"
    relevance_to_us: "low"
    summary: "Social engineering attack on employee SSO. April 27 leak deadline. Not directly relevant but vishing as initial access is trending."

stack_alerts:
  - alert: "No known active CVEs in current requirements.txt packages"
    severity: "info"
    detail: "All 8 pinned packages (flask 3.1.3, flask-limiter 4.1.1, feedparser 6.0.12, apscheduler 3.11.2, pyyaml 6.0.3, gunicorn 25.3.0, requests 2.33.1, bleach 6.3.0) have no known active CVEs as of 2026-04-25."
  - alert: "Supply chain worm pattern warrants integrity hash review"
    severity: "medium"
    detail: "Shai-Hulud npm worm highlights risk of dependency compromise. Our requirements.txt uses version pins (==) but no integrity hashes. Adding --require-hashes to pip install would harden against registry-level package tampering."
  - alert: "Python 3.x runtime — verify against recent CPython advisories"
    severity: "info"
    detail: "No critical CPython CVEs this week. Verify Railway deployment runtime version is current."

landscape_summary: >
  Supply chain attacks dominate this week — the Bitwarden CLI Shai-Hulud worm
  demonstrated self-propagating supply chain compromise at scale. CISA ED 25-03
  drives urgency for Cisco shops with the FIRESTARTER firmware backdoor. April
  Patch Tuesday brought 168 Microsoft fixes including an actively exploited
  SharePoint zero-day. No direct stack intersection for Threat-Feed but the
  supply chain pattern warrants a review of our dependency integrity controls.
