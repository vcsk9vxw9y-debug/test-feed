# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-09T12:00:00Z"
expires_at: "2026-05-12T12:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Not a Python dependency but affects the Linux host running our Railway deployment. Verify Railway's host kernel is patched. If self-hosted, apply distro vendor kernel update and reboot immediately. RHEL users apply CloudLinux KernelCare live patch or disable AF_ALG. 6 days to KEV deadline (May 15)."
  - id: "CVE-2026-43284"
    package: "linux-kernel esp4/esp6 (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag — now confirmed actively exploited in the wild (Microsoft, May 8). Upstream ESP patch merged May 8. Distro patches rolling out: AlmaLinux and CloudLinux in production repos, Ubuntu fixes available. Blacklist esp4 and esp6 kernel modules immediately on any self-managed infrastructure. Check if Railway host or any infrastructure uses IPsec tunnels."
  - id: "CVE-2026-43500"
    package: "linux-kernel rxrpc (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag rxrpc component — upstream patch still pending. Blacklist rxrpc kernel module. Only affects systems with kernel-modules-partner installed (AlmaLinux 9/10 Devel repo). Lower exposure than ESP component but same root-level impact."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree but relevant if Palo Alto firewalls are in our network path. State-sponsored exploitation (CL-STA-1132) active since April 9. CISA FCEB deadline is TODAY (May 9). Restrict User-ID Authentication Portal to trusted zones. First patch wave May 13 (4 days). UNPATCHED zero-day."
  - id: "CVE-2026-6973"
    package: "ivanti epmm (mobile device management)"
    severity: "high"
    in_requirements: false
    action: "Not in our dependency tree. Authenticated admin RCE in Ivanti EPMM. CISA KEV deadline May 10 (tomorrow). Relevant if Ivanti EPMM manages any mobile devices in our environment. Upgrade to 12.6.1.1, 12.7.0.1, or 12.8.0.1. Cloud-based Neurons for MDM not affected."
  - id: "CVE-2026-23918"
    package: "apache httpd (not in our stack directly)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree (we use gunicorn), but relevant if behind an Apache reverse proxy. If Apache is in the request path, upgrade to 2.4.67 or disable HTTP/2. PoC public — weaponization expected. Check Railway/infrastructure proxy layer."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-09.

trending_techniques:
  - name: "Perimeter device exploitation — firewall zero-days (state-sponsored)"
    mitre_id: "T1190"
    description: "CVE-2026-0300 exploited by state-sponsored cluster CL-STA-1132 since April 9. PAN-OS User-ID Authentication Portal. CISA FCEB deadline TODAY. No patch until May 13. Simultaneously, Ivanti EPMM CVE-2026-6973 added to KEV with May 10 deadline."
  - name: "Local privilege escalation via kernel memory corruption (Dirty family)"
    mitre_id: "T1068"
    description: "Two concurrent Linux kernel LPE families active: Copy Fail (CVE-2026-31431, actively exploited, chained with web RCE for ransomware) and Dirty Frag (CVE-2026-43284/43500, now confirmed actively exploited per Microsoft May 8, patches rolling out). Both yield deterministic root."
  - name: "Voice phishing (vishing) for initial access"
    mitre_id: "T1566.004"
    description: "Cushman & Wakefield breach attributed to vishing of an employee, leading to 50GB Salesforce data exfiltration. ShinyHunters and Qilin both claimed responsibility. Demonstrates social engineering bypassing technical controls."
  - name: "Supply chain compromise via package registries (Shai-Hulud campaign)"
    mitre_id: "T1195.002"
    description: "Campaign expanded from npm to PyPI (trojanized lightning 2.6.2/2.6.3). Self-propagating worm injecting malicious code into victims' own published packages. Harvests SSH keys, cloud credentials, and tokens."
  - name: "HTTP/2 protocol implementation attacks"
    mitre_id: "T1190"
    description: "CVE-2026-23918 double-free in Apache mod_http2 via HEADERS+RST_STREAM race. Working x86_64 RCE PoC public. Weaponization expected imminently."

active_campaigns:
  - actor: "CL-STA-1132 (likely state-sponsored)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root on PA-Series and VM-Series firewalls. Active since April 9 — over a month before public disclosure. Tracked by Unit 42. CISA FCEB deadline TODAY."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) for deterministic root escalation, then deploying ransomware or exfiltrating data. Exploitation confirmed increasing. Also observed chaining with Dirty Frag (Microsoft, May 8)."
  - actor: "ShinyHunters"
    targets: "Cushman & Wakefield (500K+ Salesforce records), Canvas/Instructure (275M education records), Medtronic (9M records)"
    technique: "Multi-target data extortion campaign. Vishing for initial access at Cushman & Wakefield. SaaS platform breaches for education/healthcare. 50GB Cushman dataset leaked after ransom talks failed."
  - actor: "UAT-8302 (China-nexus)"
    targets: "Government entities in South America and southeastern Europe"
    technique: "Sophisticated APT with full malware toolkit targeting sovereign infrastructure. Disclosed by Cisco Talos. Active since late 2024."
  - actor: "Shai-Hulud operators"
    targets: "npm and PyPI ecosystems, downstream consumers of compromised packages"
    technique: "Self-propagating supply chain worm. PyPI variant (lightning 2.6.2/2.6.3) harvests credentials and crypto wallets."

stack_intersection:
  - alert: "Dirty Frag (CVE-2026-43284) NOW CONFIRMED actively exploited — verify Railway platform patching status urgently. Upstream patches available, distro patches rolling out. Blacklist esp4/esp6 modules on any self-managed infrastructure."
  - alert: "Copy Fail (CVE-2026-31431) affects our Linux host kernel — verify Railway platform patching status. 6 days to KEV deadline (May 15)."
  - alert: "CVE-2026-0300 PAN-OS zero-day — CISA FCEB deadline TODAY (May 9). Check if Palo Alto firewalls are in our network path. Mitigations must be in place NOW."
  - alert: "CVE-2026-23918 Apache HTTP/2 RCE — check if Railway uses Apache in its proxy layer. Our gunicorn is not directly affected."
  - alert: "Shai-Hulud PyPI variant means our requirements.txt integrity hash gap is actively exploitable, not theoretical."

landscape_summary: "CVE-2026-0300 PAN-OS zero-day hits peak urgency — CISA FCEB deadline is TODAY (May 9) with no patch until May 13. Dirty Frag (CVE-2026-43284/43500) is now confirmed actively exploited per Microsoft (May 8), with distro patches rolling out but RxRPC upstream fix still pending — two concurrent actively-exploited Linux kernel LPE families remain in play. Ivanti EPMM CVE-2026-6973 joins the KEV catalog with a May 10 deadline, adding a third perimeter-class zero-day to the week. ShinyHunters continues its multi-sector extortion spree with the Cushman & Wakefield Salesforce leak (50GB). For our stack: no direct dependency CVEs, but the convergence of two actively-exploited Linux kernel LPEs, perimeter device exposure, and the Shai-Hulud supply chain campaign make host-level integrity and supply-chain verification the top defensive priorities."
