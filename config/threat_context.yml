# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-14T14:00:00Z"
expires_at: "2026-05-17T14:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-42945"
    package: "nginx (web infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "NGINX Rift — heap buffer overflow in ngx_http_rewrite_module. CVSS 9.2. Unauthenticated RCE via single HTTP request. Working PoC on GitHub. Affects nginx 0.6.27 through 1.30.0 and nginx Plus R32-R36. Upgrade to 1.30.1 or 1.31.0 immediately. Not in requirements.txt but nginx is likely in our deployment infrastructure (Railway, reverse proxy). Audit all nginx instances and rewrite configurations."
  - id: "CVE-2026-46300"
    package: "linux-kernel xfrm/esp-in-tcp (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Fragnesia — third Dirty Frag family LPE disclosed TODAY. Page cache corruption via ESP-in-TCP skb_try_coalesce() flaw. Deterministic root PoC published. Existing Dirty Frag mitigations (blacklist esp4/esp6/rxrpc) cover this variant. Verify mitigations in place."
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Copy Fail — actively exploited, chained with web RCE for ransomware. KEV deadline TOMORROW (May 15). Distro patches available for kernel 6.18.22, 6.19.12, 7.0. Apply and reboot TODAY. Blacklist AF_ALG on non-crypto hosts. Verify Railway host kernel is patched."
  - id: "CVE-2026-43284"
    package: "linux-kernel esp4/esp6 (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag ESP — confirmed actively exploited. Distro patches in production repos. Blacklist esp4 and esp6 kernel modules on any self-managed infrastructure. Verify Railway host patching."
  - id: "CVE-2026-43500"
    package: "linux-kernel rxrpc (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag rxrpc — upstream patch still pending as of May 14. Blacklist rxrpc kernel module."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Patches rolling out (Day 2, began May 13, full window through May 28). State-sponsored exploitation (CL-STA-1132, China-nexus) active since April 9. Apply patches immediately if Palo Alto firewalls are in our network path."
  - id: "CVE-2026-41089"
    package: "windows netlogon (infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Wormable Netlogon stack-based buffer overflow (CVSS 9.8). Patched in May Patch Tuesday. Public exploit code available — no active exploitation yet but Microsoft urges zero-day urgency. Patch DCs within 24 hours."
  - id: "CVE-2026-41096"
    package: "windows dns client (infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "DNS Client heap overflow RCE (CVSS 9.8). Patched in May Patch Tuesday. Affects virtually every Windows machine. Prioritize internet-facing Windows hosts."
  - id: "CVE-2026-45185"
    package: "exim (mail infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Dead.Letter — unauthenticated RCE via BDAT over GnuTLS. Upgrade to 4.99.3. AI-assisted exploit development confirmed."
  - id: "CVE-2026-23918"
    package: "apache httpd (not in our stack directly)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree (we use gunicorn). Upgrade to 2.4.67 or disable HTTP/2 if Apache is in our proxy layer."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-14.

trending_techniques:
  - name: "Web server infrastructure RCE via legacy code flaws"
    mitre_id: "T1190"
    description: "NGINX Rift (CVE-2026-42945) — 18-year-old heap overflow in rewrite module. Unauthenticated single-request RCE. Affects 1/3 of internet web servers. PoC on GitHub. Mass scanning expected imminently. Companion disclosure: 4 additional nginx memory corruption issues reported by depthfirst."
  - name: "Perimeter device exploitation — firewall zero-days with multi-week patch gaps"
    mitre_id: "T1190"
    description: "CVE-2026-0300 exploited by CL-STA-1132 (China-nexus) since April 9. Patches rolling out Day 2 (began May 13, window through May 28). Attackers racing to exploit remaining unpatched instances."
  - name: "Local privilege escalation via kernel memory corruption (Dirty Frag family expanding)"
    mitre_id: "T1068"
    description: "Three variants in two weeks: Dirty Frag ESP (CVE-2026-43284), Dirty Frag rxrpc (CVE-2026-43500), and Fragnesia (CVE-2026-46300, disclosed TODAY). Copy Fail (CVE-2026-31431) KEV deadline May 15 TOMORROW. All yield deterministic root."
  - name: "AI-assisted vulnerability discovery and exploit development"
    mitre_id: "T1588.005"
    description: "NGINX Rift itself discovered by depthfirst's AI system. Google TIG confirmed first AI-generated zero-day exploit in the wild. XBOW AI-assisted exploit development confirmed for Exim CVE-2026-45185. AI is accelerating both offensive and defensive vulnerability discovery."
  - name: "Coordinated zero-day disclosure by disgruntled insiders"
    mitre_id: "T1588.006"
    description: "Chaotic Eclipse has released 5 unpatched Windows zero-days in 2026 (BlueHammer, RedSun, YellowKey, GreenPlasma, plus one other). All with full PoC, no patches."
  - name: "Multi-ecosystem supply chain compromise"
    mitre_id: "T1195.002"
    description: "Shai-Hulud self-propagating worm active on npm and PyPI. GemStuffer campaign 500+ malicious packages on RubyGems — registry forced to suspend signups. Three major package ecosystems simultaneously compromised."

active_campaigns:
  - actor: "Unknown (mass scanning expected)"
    targets: "All internet-exposed nginx servers with rewrite module configurations"
    technique: "CVE-2026-42945 NGINX Rift exploitation. Single HTTP request heap overflow for RCE/DoS. Working PoC published by depthfirst on GitHub. The vulnerable configuration pattern is extremely common. Mass scanning and automated exploitation expected imminently."
  - actor: "CL-STA-1132 (China-nexus, likely state-sponsored)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root on PA-Series and VM-Series firewalls. Active since April 9. Patches now available — exploitation accelerating against laggards."
  - actor: "Chaotic Eclipse (disgruntled former Microsoft employee)"
    targets: "All Windows 11 users"
    technique: "Serial zero-day disclosure with full PoC. YellowKey (BitLocker bypass) and GreenPlasma (CTFMON LPE to SYSTEM) dropped May 12-13. Five unpatched zero-days in 2026."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) and Dirty Frag (CVE-2026-43284) for deterministic root escalation, then deploying ransomware."
  - actor: "Nitrogen ransomware group"
    targets: "Foxconn manufacturing facilities (North America)"
    technique: "Confirmed breach of Foxconn U.S. facilities (Mount Pleasant, WI and Houston, TX). Claims 8TB / 11M files stolen including data related to Apple, Dell, Google, Intel, Nvidia projects. Double extortion. Factories resuming production."
  - actor: "Shai-Hulud + GemStuffer operators"
    targets: "npm, PyPI, and RubyGems ecosystems"
    technique: "Self-propagating supply chain worm (Shai-Hulud) and registry abuse for data exfiltration (GemStuffer). RubyGems forced to suspend new signups."
  - actor: "AI-enabled threat actors (China/DPRK-linked)"
    targets: "Web-based admin tools with 2FA"
    technique: "Using AI models for zero-day vulnerability discovery and exploit generation. Google thwarted mass exploitation attempt. First confirmed AI-generated zero-day in the wild."

stack_intersection:
  - alert: "NGINX Rift (CVE-2026-42945) — verify nginx version in Railway deployment and any reverse proxy layer. If nginx < 1.30.1, upgrade immediately. Audit rewrite rules for vulnerable pattern."
  - alert: "Fragnesia (CVE-2026-46300) disclosed TODAY — third Dirty Frag variant. Existing esp4/esp6/rxrpc blacklist mitigations cover this. Verify mitigations still in place after any kernel updates."
  - alert: "Copy Fail (CVE-2026-31431) KEV deadline is TOMORROW (May 15). Verify Railway platform patching status immediately."
  - alert: "CVE-2026-0300 PAN-OS patches rolling out Day 2. Check if Palo Alto firewalls are in our network path and apply immediately."
  - alert: "CVE-2026-41089 wormable Netlogon RCE with public exploit code — if any Windows DCs are in our environment, patch within 24 hours."
  - alert: "Shai-Hulud PyPI variant + GemStuffer RubyGems campaign = multi-ecosystem supply chain attacks active. Requirements.txt integrity hash gap remains exploitable."

landscape_summary: "NGINX Rift (CVE-2026-42945) dominates today's landscape — an 18-year-old unauthenticated RCE affecting one-third of the internet's web servers with a public PoC on GitHub. Mass scanning is expected imminently. Concurrently, Fragnesia (CVE-2026-46300) is the third Linux kernel LPE in the Dirty Frag family in two weeks, disclosed today with a working PoC. The Copy Fail KEV deadline hits tomorrow (May 15) with active ransomware chains in the wild. PAN-OS CVE-2026-0300 patches continue rolling out Day 2 while state-sponsored exploitation continues. Netlogon CVE-2026-41089 has public wormable exploit code. Foxconn confirmed a Nitrogen ransomware breach affecting North American factories with claims of Apple/Nvidia data theft. For our stack: nginx version in our deployment infrastructure is the most urgent check — if running < 1.30.1, we are directly exposed to trivial unauthenticated RCE. Copy Fail KEV deadline tomorrow requires Railway platform verification."
