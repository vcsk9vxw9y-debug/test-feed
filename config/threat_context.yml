# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-11T12:00:00Z"
expires_at: "2026-05-14T12:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Not a Python dependency but affects the Linux host running our Railway deployment. Verify Railway's host kernel is patched. If self-hosted, apply distro vendor kernel update and reboot immediately. RHEL users apply CloudLinux KernelCare live patch or disable AF_ALG. 4 days to KEV deadline (May 15)."
  - id: "CVE-2026-43284"
    package: "linux-kernel esp4/esp6 (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag — confirmed actively exploited (Microsoft, May 8). Upstream ESP patch merged May 8. Distro patches in production repos for AlmaLinux, CloudLinux, Ubuntu. Blacklist esp4 and esp6 kernel modules immediately on any self-managed infrastructure. Check if Railway host or any infrastructure uses IPsec tunnels."
  - id: "CVE-2026-43500"
    package: "linux-kernel rxrpc (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag rxrpc component — upstream patch still pending as of May 11. Blacklist rxrpc kernel module. Only affects systems with kernel-modules-partner installed (AlmaLinux 9/10 Devel repo). Lower exposure than ESP component but same root-level impact."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree but relevant if Palo Alto firewalls are in our network path. State-sponsored exploitation (CL-STA-1132, likely China-nexus) active since April 9. CISA FCEB deadline PASSED (May 9). Restrict User-ID Authentication Portal to trusted zones. First patch wave May 13 (2 days). UNPATCHED zero-day."
  - id: "CVE-2026-42208"
    package: "litellm (AI LLM proxy)"
    severity: "critical"
    in_requirements: false
    action: "Not in our requirements.txt but relevant if LiteLLM is used anywhere in the AI infrastructure. Pre-auth SQL injection (CVSS 9.8) exploited within 36 hours of disclosure. CISA KEV deadline TODAY (May 11). Upgrade to v1.83.10-stable. Rotate all API keys managed by LiteLLM."
  - id: "CVE-2026-41940"
    package: "cpanel & whm (hosting infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree. CVSS 9.8 auth bypass exploited since February 2026. 1.5M exposed instances. Patch available since April 28. Relevant if any infrastructure uses cPanel. Multi-actor exploitation targeting govts and MSPs."
  - id: "CVE-2026-6973"
    package: "ivanti epmm (mobile device management)"
    severity: "high"
    in_requirements: false
    action: "Not in our dependency tree. Authenticated admin RCE in Ivanti EPMM. CISA KEV deadline passed (May 10). Relevant if Ivanti EPMM manages any mobile devices in our environment. Upgrade to 12.6.1.1, 12.7.0.1, or 12.8.0.1. Cloud-based Neurons for MDM not affected."
  - id: "CVE-2026-23918"
    package: "apache httpd (not in our stack directly)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree (we use gunicorn), but relevant if behind an Apache reverse proxy. If Apache is in the request path, upgrade to 2.4.67 or disable HTTP/2. PoC public — weaponization expected. Check Railway/infrastructure proxy layer."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-11.

trending_techniques:
  - name: "Perimeter device exploitation — firewall and MDM zero-days (state-sponsored)"
    mitre_id: "T1190"
    description: "CVE-2026-0300 exploited by CL-STA-1132 (likely China-nexus, Earthworm/ReverseSocks5 tooling) since April 9. CISA deadline passed. No patch until May 13. Ivanti EPMM CVE-2026-6973 KEV deadline also passed (May 10). cPanel CVE-2026-41940 mass-exploited across five countries."
  - name: "Local privilege escalation via kernel memory corruption (Dirty family)"
    mitre_id: "T1068"
    description: "Two concurrent Linux kernel LPE families active: Copy Fail (CVE-2026-31431, actively exploited, chained with web RCE for ransomware, KEV deadline May 15) and Dirty Frag (CVE-2026-43284/43500, confirmed actively exploited per Microsoft May 8, ESP patches rolling out, rxrpc still unpatched). Both yield deterministic root."
  - name: "AI infrastructure exploitation — LLM proxy targeting"
    mitre_id: "T1190"
    description: "CVE-2026-42208 pre-auth SQL injection in LiteLLM Proxy exploited within 36 hours of disclosure. Exposes API keys and LLM provider credentials. CISA KEV deadline TODAY (May 11). Signals growing attacker interest in AI infrastructure as high-value targets."
  - name: "Voice phishing (vishing) for initial access"
    mitre_id: "T1566.004"
    description: "Cushman & Wakefield breach attributed to vishing of an employee, leading to 50GB Salesforce data exfiltration. ShinyHunters and Qilin both claimed responsibility. Demonstrates social engineering bypassing technical controls."
  - name: "Supply chain compromise via package registries (Shai-Hulud campaign)"
    mitre_id: "T1195.002"
    description: "Campaign expanded from npm to PyPI (trojanized lightning 2.6.2/2.6.3). Self-propagating worm injecting malicious code into victims' own published packages. Harvests SSH keys, cloud credentials, and tokens."

active_campaigns:
  - actor: "CL-STA-1132 (China-nexus, likely state-sponsored)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root on PA-Series and VM-Series firewalls. Active since April 9 — over a month before public disclosure. Earthworm and ReverseSocks5 tooling overlap with Volt Typhoon and APT41. CISA FCEB deadline passed May 9. Patch expected May 13."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) and Dirty Frag (CVE-2026-43284) for deterministic root escalation, then deploying ransomware or exfiltrating data. Exploitation confirmed increasing per Microsoft May 8 report."
  - actor: "Multiple threat actors (cPanel campaign)"
    targets: "Governments (Philippines, Laos, Guam), MSPs, hosting providers across five countries"
    technique: "Mass-exploiting CVE-2026-41940 cPanel auth bypass for server takeover. Deploying ransomware, botnet malware, and C2 frameworks. Exploitation active since February 2026, escalated to multi-actor in May."
  - actor: "ShinyHunters"
    targets: "Cushman & Wakefield (500K+ Salesforce records), Canvas/Instructure (275M education records), Medtronic (9M records)"
    technique: "Multi-target data extortion campaign. Vishing for initial access at Cushman & Wakefield. SaaS platform breaches for education/healthcare. 50GB Cushman dataset leaked after ransom talks failed."
  - actor: "UAT-8302 (China-nexus)"
    targets: "Government entities in South America and southeastern Europe"
    technique: "Sophisticated APT with full malware toolkit targeting sovereign infrastructure. Disclosed by Cisco Talos. Active since late 2024."
  - actor: "Shai-Hulud operators"
    targets: "npm and PyPI ecosystems, downstream consumers of compromised packages"
    technique: "Self-propagating supply chain worm. PyPI variant (lightning 2.6.2/2.6.3) harvests credentials and crypto wallets."

stack_intersection:
  - alert: "Dirty Frag (CVE-2026-43284) confirmed actively exploited — verify Railway platform patching status. ESP upstream patches available, distro patches in production repos. Blacklist esp4/esp6 modules on any self-managed infrastructure."
  - alert: "Copy Fail (CVE-2026-31431) affects our Linux host kernel — verify Railway platform patching status. 4 days to KEV deadline (May 15)."
  - alert: "CVE-2026-0300 PAN-OS zero-day — CISA FCEB deadline passed (May 9). Check if Palo Alto firewalls are in our network path. First patch May 13 (2 days)."
  - alert: "CVE-2026-42208 LiteLLM — KEV deadline TODAY. If LiteLLM is deployed anywhere in our AI stack, upgrade to v1.83.10-stable and rotate credentials immediately."
  - alert: "CVE-2026-23918 Apache HTTP/2 RCE — check if Railway uses Apache in its proxy layer. Our gunicorn is not directly affected."
  - alert: "Shai-Hulud PyPI variant means our requirements.txt integrity hash gap is actively exploitable, not theoretical."

landscape_summary: "The PAN-OS zero-day (CVE-2026-0300) enters its most critical phase — CISA FCEB deadline passed May 9 with no patch until May 13 (2 days), while CL-STA-1132 (now assessed as likely China-nexus) continues exploiting exposed firewalls. LiteLLM CVE-2026-42208 hits its CISA KEV deadline TODAY (May 11), signaling growing attacker interest in AI infrastructure. Two concurrent actively-exploited Linux kernel LPEs remain in play (Copy Fail + Dirty Frag), with Dirty Frag rxrpc patches still pending upstream. The cPanel auth bypass (CVE-2026-41940) has escalated to multi-actor mass exploitation targeting governments and MSPs across five countries. For our stack: no direct dependency CVEs, but the convergence of Linux kernel LPEs, AI infrastructure targeting (LiteLLM), perimeter device exposure (PAN-OS), and the Shai-Hulud supply chain campaign make host-level integrity, AI stack auditing, and supply-chain verification the top defensive priorities."
