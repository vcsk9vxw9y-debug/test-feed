# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-06T12:00:00Z"
expires_at: "2026-05-09T12:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Not a Python dependency but affects the Linux host running our Railway deployment. Verify Railway's host kernel is patched. If self-hosted, apply distro vendor kernel update and reboot immediately. RHEL users apply CloudLinux KernelCare live patch or disable AF_ALG. 9 days to KEV deadline."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree but relevant if Palo Alto firewalls are in our network path. Check if any PA-Series or VM-Series firewalls protect our infrastructure. If so, restrict User-ID Authentication Portal to trusted zones and plan for May 13 patch. UNPATCHED zero-day."
  - id: "CVE-2026-23918"
    package: "apache httpd (not in our stack directly)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree (we use gunicorn), but relevant if behind an Apache reverse proxy. If Apache is in the request path, upgrade to 2.4.67 or disable HTTP/2. Check Railway/infrastructure proxy layer."
  - id: "CVE-2026-4670"
    package: "moveit-automation (not in our stack)"
    severity: "critical"
    in_requirements: false
    action: "No action required — not in our dependency tree. Relevant only if MOVEit Automation is used for file transfers in the organization. Upgrade to patched version if present."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-06.

trending_techniques:
  - name: "Perimeter device exploitation — firewall zero-days"
    mitre_id: "T1190"
    description: "CVE-2026-0300 targets Palo Alto PAN-OS User-ID Authentication Portal. Pattern: attackers targeting perimeter security devices for network-level access. Unpatched zero-day with 7 days until fix."
  - name: "Local privilege escalation chained with web RCE for ransomware"
    mitre_id: "T1068"
    description: "Copy Fail (CVE-2026-31431) continues being chained with web application and VPN RCE as a reliable post-exploitation primitive. 9 days to KEV deadline."
  - name: "HTTP/2 protocol implementation attacks"
    mitre_id: "T1190"
    description: "CVE-2026-23918 double-free in Apache mod_http2 via HEADERS+RST_STREAM race. Working x86_64 RCE PoC public. Weaponization expected imminently."
  - name: "Supply chain compromise via third-party analytics vendors"
    mitre_id: "T1199"
    description: "ShinyHunters breached Medtronic and Vimeo via third-party vendors (Anodot for Vimeo). Trend of targeting smaller vendors with access to high-value customer data."
  - name: "Supply chain compromise via package registries (Shai-Hulud campaign)"
    mitre_id: "T1195.002"
    description: "Campaign expanded from npm to PyPI (trojanized lightning 2.6.2/2.6.3). Self-propagating worm injecting malicious code into victims' own published packages. Harvests SSH keys, cloud credentials, and tokens."

active_campaigns:
  - actor: "Unknown (targeting Palo Alto firewalls)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root on PA-Series and VM-Series firewalls. Limited but confirmed active exploitation. No patch available until May 13."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) for deterministic root escalation, then deploying ransomware or exfiltrating data. Exploitation confirmed increasing."
  - actor: "ShinyHunters"
    targets: "Healthcare (Medtronic — 9M records), SaaS platforms (Vimeo via Anodot)"
    technique: "Targeting third-party vendors with access to primary victim data. Exfiltrating terabytes, threatening leaks. Ransom payments suspected based on leak site removals."
  - actor: "UAT-8302 (China-nexus)"
    targets: "Government entities in South America and southeastern Europe"
    technique: "Sophisticated APT with full malware toolkit targeting sovereign infrastructure. Disclosed by Cisco Talos. Active since late 2024."
  - actor: "Shai-Hulud operators"
    targets: "npm and PyPI ecosystems, downstream consumers of compromised packages"
    technique: "Self-propagating supply chain worm. PyPI variant (lightning 2.6.2/2.6.3) harvests credentials and crypto wallets."

stack_intersection:
  - alert: "Copy Fail affects our Linux host kernel — verify Railway platform patching status. 9 days to KEV deadline."
  - alert: "CVE-2026-0300 PAN-OS zero-day — check if Palo Alto firewalls are in our network path. If so, mitigate immediately. No patch until May 13."
  - alert: "CVE-2026-23918 Apache HTTP/2 RCE — check if Railway uses Apache in its proxy layer. Our gunicorn is not directly affected."
  - alert: "Shai-Hulud PyPI variant means our requirements.txt integrity hash gap is actively exploitable, not theoretical."
  - alert: "ShinyHunters third-party vendor pattern relevant to any analytics/monitoring services we integrate with."

landscape_summary: "NEW: CVE-2026-0300 — actively exploited, unpatched Palo Alto PAN-OS zero-day (CVSS 9.3) gives unauthenticated attackers root on firewalls. No fix until May 13; mitigate by restricting or disabling User-ID Authentication Portal. Copy Fail (CVE-2026-31431) exploitation continues expanding with 9 days to KEV deadline. MOVEit Automation (CVE-2026-4670, CVSS 9.8) auth bypass disclosed May 4 — no wild exploitation yet but MOVEit's history warrants urgency. Apache HTTP/2 RCE (CVE-2026-23918) has a working PoC and weaponization is expected imminently. For our stack: no direct dependency CVEs, but host kernel patching, perimeter device exposure, and pip integrity hashes remain the closest intersection points."
