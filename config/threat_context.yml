# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-13T12:00:00Z"
expires_at: "2026-05-16T12:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Copy Fail — actively exploited, chained with web RCE for ransomware. KEV deadline May 15 (2 days). Verify Railway host kernel is patched. If self-hosted, apply distro kernel update and reboot. Blacklist AF_ALG on non-IPsec hosts."
  - id: "CVE-2026-43284"
    package: "linux-kernel esp4/esp6 (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag ESP — confirmed actively exploited (Microsoft, May 8). Distro patches in production repos for AlmaLinux, CloudLinux, Ubuntu. Blacklist esp4 and esp6 kernel modules on any self-managed infrastructure. Verify Railway host patching."
  - id: "CVE-2026-43500"
    package: "linux-kernel rxrpc (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag rxrpc — upstream patch still pending as of May 13. Blacklist rxrpc kernel module. Only affects systems with kernel-modules-partner installed."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "PATCHES RELEASING TODAY (May 13). Not in our dependency tree but relevant if Palo Alto firewalls are in our network path. State-sponsored exploitation (CL-STA-1132, China-nexus) active since April 9. Apply patches immediately. Full patch window through May 28."
  - id: "CVE-2026-41089"
    package: "windows netlogon (infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Wormable Netlogon stack-based buffer overflow (CVSS 9.8). Patched in May Patch Tuesday. Not in our stack directly but affects any Windows domain controllers in the environment. Patch DCs within 24 hours. No active exploitation yet but wormable = urgent."
  - id: "CVE-2026-41096"
    package: "windows dns client (infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "DNS Client heap overflow RCE (CVSS 9.8). Patched in May Patch Tuesday. Affects virtually every Windows machine. Prioritize internet-facing Windows hosts. Not in our direct stack but relevant for any Windows infrastructure."
  - id: "CVE-2026-45185"
    package: "exim (mail infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Dead.Letter — unauthenticated RCE via BDAT over GnuTLS. Affects Exim 4.97-4.99.2 on GnuTLS builds (Debian/Ubuntu default). Upgrade to 4.99.3. Not in our dependency tree but relevant if Exim handles mail for our domain. AI-assisted exploit development confirmed."
  - id: "CVE-2026-2291"
    package: "dnsmasq (network infrastructure)"
    severity: "high"
    in_requirements: false
    action: "Part of 6-CVE dnsmasq cluster (DNS poisoning, heap RCE, privilege escalation). Upgrade to 2.92rel2. Check containers and embedded devices for dnsmasq instances. Not in our requirements.txt but ubiquitous in infrastructure."
  - id: "CVE-2026-23918"
    package: "apache httpd (not in our stack directly)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree (we use gunicorn), but relevant if behind an Apache reverse proxy. Upgrade to 2.4.67 or disable HTTP/2. PoC public. Check Railway/infrastructure proxy layer."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-13.

trending_techniques:
  - name: "Perimeter device exploitation — firewall zero-days with delayed patches"
    mitre_id: "T1190"
    description: "CVE-2026-0300 exploited by CL-STA-1132 (China-nexus) since April 9 with 7-day patch gap after disclosure. Patches releasing today (May 13). Attackers exploiting the window between CISA deadline and patch availability."
  - name: "Wormable infrastructure RCE via protocol-level flaws"
    mitre_id: "T1210"
    description: "CVE-2026-41089 Netlogon wormable RCE (CVSS 9.8) and CVE-2026-41096 DNS Client RCE (CVSS 9.8) patched today. No active exploitation yet but both are pre-auth, no-interaction, critical-infrastructure targets. Weaponization expected rapidly."
  - name: "Local privilege escalation via kernel memory corruption (Dirty family)"
    mitre_id: "T1068"
    description: "Copy Fail (CVE-2026-31431, KEV deadline May 15) and Dirty Frag (CVE-2026-43284/43500) continue as concurrent Linux kernel LPE families. Both yield deterministic root. ESP patches available, rxrpc still unpatched upstream."
  - name: "Mail server exploitation via TLS implementation flaws"
    mitre_id: "T1190"
    description: "CVE-2026-45185 Dead.Letter — unauthenticated RCE in Exim via BDAT/GnuTLS use-after-free. AI-assisted exploit development confirmed. Concentrated exposure on Debian/Ubuntu. Mass scanning expected."
  - name: "Supply chain compromise via package registries (Shai-Hulud + GemStuffer)"
    mitre_id: "T1195.002"
    description: "Shai-Hulud self-propagating worm expanded from npm to PyPI (trojanized lightning 2.6.2/2.6.3). GemStuffer campaign planted 150+ gems on RubyGems using registry as data exfiltration channel. Multi-ecosystem supply chain attacks now concurrent."

active_campaigns:
  - actor: "CL-STA-1132 (China-nexus, likely state-sponsored)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root on PA-Series and VM-Series firewalls. Active since April 9. Earthworm/ReverseSocks5 tooling overlap with Volt Typhoon and APT41. Patches releasing today — expect exploitation to accelerate before patch adoption."
  - actor: "APT28 (Fancy Bear, Russia)"
    targets: "Organizations in Ukraine and EU member countries"
    technique: "Exploiting Windows Shell CVE-2026-32202 (incomplete patch for CVE-2026-21510) for NTLMv2 credential hash theft without user interaction. Active since December 2025. KEV deadline was May 12."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) and Dirty Frag (CVE-2026-43284) for deterministic root escalation, then deploying ransomware."
  - actor: "ShinyHunters"
    targets: "Instructure/Canvas (275M education records), Cushman & Wakefield (500K+ Salesforce records)"
    technique: "Multi-target data extortion. Vishing for initial access at Cushman & Wakefield. SaaS platform breaches for education sector. Active negotiations and data leaks ongoing."
  - actor: "UAT-8302 (China-nexus)"
    targets: "Government entities in South America and southeastern Europe"
    technique: "Sophisticated APT with full malware toolkit targeting sovereign infrastructure. Disclosed by Cisco Talos. Active since late 2024."
  - actor: "Shai-Hulud + GemStuffer operators"
    targets: "npm, PyPI, and RubyGems ecosystems"
    technique: "Self-propagating supply chain worm (Shai-Hulud) and registry abuse for data exfiltration (GemStuffer). Multi-ecosystem. Harvests SSH keys, cloud credentials, crypto wallets."

stack_intersection:
  - alert: "Dirty Frag (CVE-2026-43284) confirmed actively exploited — verify Railway platform patching status. ESP distro patches available. Blacklist esp4/esp6 modules on any self-managed infrastructure."
  - alert: "Copy Fail (CVE-2026-31431) affects Linux host kernel — KEV deadline in 2 days (May 15). Verify Railway platform patching."
  - alert: "CVE-2026-0300 PAN-OS patches releasing TODAY. Check if Palo Alto firewalls are in our network path and apply immediately."
  - alert: "CVE-2026-41089 wormable Netlogon RCE — if any Windows DCs are in our environment, patch within 24 hours."
  - alert: "Shai-Hulud PyPI variant + GemStuffer campaign = multi-ecosystem supply chain attacks active. Requirements.txt integrity hash gap remains exploitable."
  - alert: "CVE-2026-45185 Exim Dead.Letter — check if Exim handles mail for our domain. AI-assisted exploit = faster weaponization timeline."

landscape_summary: "Today is the highest-density patch day in weeks. PAN-OS CVE-2026-0300 patches are finally releasing after 34 days of state-sponsored exploitation by China-nexus CL-STA-1132 — organizations must apply immediately as attackers will race to exploit remaining unpatched instances. Microsoft's May Patch Tuesday adds 138 CVEs including a wormable Netlogon RCE (CVE-2026-41089, CVSS 9.8) and a DNS Client RCE (CVE-2026-41096, CVSS 9.8) that together threaten every Windows domain. Exim Dead.Letter (CVE-2026-45185) and the dnsmasq 6-CVE cluster create additional infrastructure exposure. Linux kernel LPEs (Copy Fail + Dirty Frag) remain actively exploited with the Copy Fail KEV deadline just 2 days away. For our stack: no direct dependency CVEs, but the convergence of patch-day urgency across PAN-OS, Windows, Exim, and dnsmasq alongside active kernel LPEs and multi-ecosystem supply chain attacks (Shai-Hulud + GemStuffer) makes today a critical patching and validation day."
