# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-15T12:00:00Z"
expires_at: "2026-05-18T12:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-42945"
    package: "nginx (web infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "NGINX Rift — heap buffer overflow in ngx_http_rewrite_module. CVSS 9.2. Unauthenticated RCE via single HTTP request. Working PoC on GitHub. Day 3 of disclosure, no confirmed mass exploitation yet but scanning imminent. Affects nginx 0.6.27 through 1.30.0 and nginx Plus R32-R36. Upgrade to 1.30.1 or 1.31.0 immediately. Not in requirements.txt but nginx is likely in our deployment infrastructure. Audit all nginx instances and rewrite configurations."
  - id: "CVE-2026-20182"
    package: "cisco catalyst sd-wan (network infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "CVSS 10.0 authentication bypass in Cisco Catalyst SD-WAN Controller and Manager. Actively exploited by UAT-8616. Added to CISA KEV May 14 with Emergency Directive 26-03. Deadline May 17. Check if Cisco SD-WAN is in our network path and patch immediately."
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Copy Fail — actively exploited, chained with web RCE for ransomware. KEV deadline is TODAY (May 15). Distro patches available for kernel 6.18.22, 6.19.12, 7.0. Apply and reboot. Blacklist AF_ALG on non-crypto hosts. Verify Railway host kernel is patched."
  - id: "CVE-2026-46300"
    package: "linux-kernel xfrm/esp-in-tcp (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Fragnesia — third Dirty Frag family LPE. Page cache corruption via ESP-in-TCP. Deterministic root PoC published. Existing Dirty Frag mitigations (blacklist esp4/esp6/rxrpc) cover this variant. Verify mitigations in place."
  - id: "CVE-2026-43284"
    package: "linux-kernel esp4/esp6 (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag ESP — confirmed actively exploited. Distro patches in production repos. Blacklist esp4 and esp6 kernel modules on any self-managed infrastructure."
  - id: "CVE-2026-43500"
    package: "linux-kernel rxrpc (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Dirty Frag rxrpc — upstream patch status should be verified. Blacklist rxrpc kernel module."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Patches rolling out (Day 3, began May 13, window through May 28). State-sponsored exploitation (CL-STA-1132, China-nexus) active since April 9. Apply patches immediately if Palo Alto firewalls are in our network path."
  - id: "CVE-2026-41089"
    package: "windows netlogon (infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Wormable Netlogon stack-based buffer overflow (CVSS 9.8). Patched in May Patch Tuesday. Public exploit code available. Patch DCs within 24 hours."
  - id: "CVE-2026-41096"
    package: "windows dns client (infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "DNS Client heap overflow RCE (CVSS 9.8). Patched in May Patch Tuesday. Prioritize internet-facing Windows hosts."
  - id: "CVE-2026-45185"
    package: "exim (mail infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Dead.Letter — unauthenticated RCE via BDAT over GnuTLS. Upgrade to 4.99.3. AI-assisted exploit development confirmed."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-15.

trending_techniques:
  - name: "Web server infrastructure RCE via legacy code flaws"
    mitre_id: "T1190"
    description: "NGINX Rift (CVE-2026-42945) — 18-year-old heap overflow in rewrite module. Day 3, no confirmed mass exploitation yet but PoC is public and scanning expected. Companion: 4 additional nginx memory corruption issues reported by depthfirst."
  - name: "Network infrastructure authentication bypass (SD-WAN)"
    mitre_id: "T1190"
    description: "CVE-2026-20182 — CVSS 10.0 auth bypass in Cisco Catalyst SD-WAN. UAT-8616 actively exploiting to gain admin access, manipulate NETCONF, and pivot across SD-WAN fabric. CISA Emergency Directive 26-03 issued."
  - name: "Perimeter device exploitation — firewall zero-days with multi-week patch gaps"
    mitre_id: "T1190"
    description: "CVE-2026-0300 exploited by CL-STA-1132 (China-nexus) since April 9. Patches Day 3 (began May 13, window through May 28). Attackers racing to exploit remaining unpatched instances."
  - name: "Local privilege escalation via kernel memory corruption (Dirty Frag family)"
    mitre_id: "T1068"
    description: "Three Dirty Frag variants plus Copy Fail (CVE-2026-31431, KEV deadline TODAY May 15). All yield deterministic root. Being chained with web/VPN RCE for ransomware deployment."
  - name: "AI-assisted vulnerability discovery and exploit development"
    mitre_id: "T1588.005"
    description: "NGINX Rift discovered by depthfirst's AI system. Google TIG confirmed first AI-generated zero-day exploit in the wild (2FA bypass). AI accelerating both offensive and defensive vulnerability discovery across multiple ecosystems."
  - name: "Coordinated zero-day disclosure by disgruntled insiders"
    mitre_id: "T1588.006"
    description: "Chaotic Eclipse has released 5 unpatched Windows zero-days in 2026 including YellowKey (BitLocker bypass) and GreenPlasma (CTFMON LPE to SYSTEM) dropped May 12-13. All with full PoC, no patches."

active_campaigns:
  - actor: "UAT-8616 (sophisticated, tracked by Cisco Talos)"
    targets: "Organizations with Cisco Catalyst SD-WAN Controller and Manager deployments"
    technique: "Exploiting CVE-2026-20182 (CVSS 10.0) authentication bypass. Masquerading as vHub peers to skip certificate verification. Adding SSH keys, modifying NETCONF configurations, escalating to root. Active since at least 2023 against Cisco SD-WAN infrastructure."
  - actor: "Unknown (mass scanning expected)"
    targets: "All internet-exposed nginx servers with rewrite module configurations"
    technique: "CVE-2026-42945 NGINX Rift exploitation. Single HTTP request heap overflow for RCE/DoS. Working PoC published on GitHub. Day 3 — mass automated exploitation expected."
  - actor: "CL-STA-1132 (China-nexus, likely state-sponsored)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root. Active since April 9. Exploitation accelerating against unpatched instances as patches roll out."
  - actor: "ShinyHunters (ransomware group)"
    targets: "SaaS platforms — Canvas/Instructure (education), Cushman & Wakefield (commercial real estate)"
    technique: "Breached Canvas LMS affecting 8,809 institutions worldwide. Also hit Cushman & Wakefield exposing 500K+ Salesforce records. Double extortion."
  - actor: "Chaotic Eclipse (disgruntled former Microsoft employee)"
    targets: "All Windows 11 users"
    technique: "Serial zero-day disclosure with full PoC. YellowKey (BitLocker bypass) and GreenPlasma (CTFMON LPE to SYSTEM) dropped May 12-13. Five unpatched zero-days in 2026."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) and Dirty Frag family for deterministic root escalation, then deploying ransomware."
  - actor: "Shai-Hulud + GemStuffer operators"
    targets: "npm, PyPI, and RubyGems ecosystems"
    technique: "Self-propagating supply chain worm (Shai-Hulud) and registry abuse for data exfiltration (GemStuffer). RubyGems forced to suspend new signups."

stack_intersection:
  - alert: "NGINX Rift (CVE-2026-42945) — Day 3. Verify nginx version in Railway deployment and any reverse proxy layer. If nginx < 1.30.1, upgrade immediately. Audit rewrite rules for vulnerable pattern."
  - alert: "CVE-2026-20182 Cisco SD-WAN (CVSS 10.0) — check if Cisco SD-WAN is in our network path. CISA Emergency Directive deadline May 17."
  - alert: "Copy Fail (CVE-2026-31431) KEV deadline is TODAY (May 15). Verify Railway platform patching status immediately. Federal agencies non-compliant after today."
  - alert: "Dirty Frag family (3 variants) — verify esp4/esp6/rxrpc blacklist mitigations still in place after any kernel updates."
  - alert: "CVE-2026-0300 PAN-OS patches Day 3. Check if Palo Alto firewalls are in our network path and apply immediately."
  - alert: "Shai-Hulud PyPI variant active. Requirements.txt integrity hash gap remains exploitable. Consider adding hash-checking to dependency pipeline."

landscape_summary: "Day 3 of NGINX Rift (CVE-2026-42945) remains the dominant story — unauthenticated RCE affecting 1/3 of internet web servers with a public PoC but no confirmed mass exploitation yet. NEW today: CVE-2026-20182 Cisco Catalyst SD-WAN authentication bypass (CVSS 10.0) was added to CISA KEV yesterday with Emergency Directive 26-03 and a May 17 deadline — UAT-8616 is actively exploiting it to manipulate SD-WAN fabric configurations. Copy Fail (CVE-2026-31431) KEV deadline hits TODAY with active ransomware chains in the wild. PAN-OS CVE-2026-0300 patches continue Day 3. ShinyHunters breached Canvas LMS affecting 8,809 educational institutions — the largest education sector breach on record. For our stack: nginx version verification remains the most urgent action. Copy Fail KEV compliance is due today. Check for Cisco SD-WAN exposure."
