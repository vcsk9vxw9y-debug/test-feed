# Threat Context — generated by Peter Parker during daily curation.
# Consumed by Aegis for situational awareness. Do not manually edit.
active: true
generated_at: "2026-05-07T12:00:00Z"
expires_at: "2026-05-10T12:00:00Z"

# Active CVEs relevant to the Threat-Feed stack
stack_cves:
  - id: "CVE-2026-31431"
    package: "linux-kernel (host OS)"
    severity: "critical"
    in_requirements: false
    action: "Not a Python dependency but affects the Linux host running our Railway deployment. Verify Railway's host kernel is patched. If self-hosted, apply distro vendor kernel update and reboot immediately. RHEL users apply CloudLinux KernelCare live patch or disable AF_ALG. 8 days to KEV deadline."
  - id: "CVE-2026-0300"
    package: "palo-alto pan-os (perimeter infrastructure)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree but relevant if Palo Alto firewalls are in our network path. State-sponsored exploitation (CL-STA-1132) active since April 9. CISA FCEB deadline May 9 (2 days). Restrict User-ID Authentication Portal to trusted zones and plan for May 13 patch. UNPATCHED zero-day."
  - id: "CVE-2026-23918"
    package: "apache httpd (not in our stack directly)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree (we use gunicorn), but relevant if behind an Apache reverse proxy. If Apache is in the request path, upgrade to 2.4.67 or disable HTTP/2. Check Railway/infrastructure proxy layer."
  - id: "CVE-2026-4670"
    package: "moveit-automation (not in our stack)"
    severity: "critical"
    in_requirements: false
    action: "No action required — not in our dependency tree. Relevant only if MOVEit Automation is used for file transfers in the organization. Upgrade to patched version if present."
  - id: "CVE-2026-7482"
    package: "ollama (not in our stack)"
    severity: "critical"
    in_requirements: false
    action: "Not in our dependency tree. Relevant if Ollama is used for local AI inference. Upgrade to 0.17.1. Restrict network access — Ollama defaults to no authentication on all interfaces."

# Cross-reference against requirements.txt:
# flask==3.1.3, flask-limiter==4.1.1, feedparser==6.0.12, apscheduler==3.11.2,
# pyyaml==6.0.3, gunicorn==25.3.0, requests==2.33.1, bleach==6.3.0,
# python-dateutil==2.9.0.post0
# No known active CVEs in any pinned dependency as of 2026-05-07.

trending_techniques:
  - name: "Perimeter device exploitation — firewall zero-days (state-sponsored)"
    mitre_id: "T1190"
    description: "CVE-2026-0300 exploited by state-sponsored cluster CL-STA-1132 since April 9. Palo Alto PAN-OS User-ID Authentication Portal. Unpatched zero-day with 6 days until fix. CISA FCEB deadline May 9."
  - name: "Local privilege escalation chained with web RCE for ransomware"
    mitre_id: "T1068"
    description: "Copy Fail (CVE-2026-31431) continues being chained with web application and VPN RCE as a reliable post-exploitation primitive. 8 days to KEV deadline."
  - name: "AI infrastructure exploitation — memory disclosure"
    mitre_id: "T1190"
    description: "Bleeding Llama (CVE-2026-7482) exploits Ollama's unauthenticated API to leak process memory containing chat history, PII, credentials. 300K internet-exposed instances. Patch in 0.17.1 but adoption lagging."
  - name: "HTTP/2 protocol implementation attacks"
    mitre_id: "T1190"
    description: "CVE-2026-23918 double-free in Apache mod_http2 via HEADERS+RST_STREAM race. Working x86_64 RCE PoC public. Weaponization expected imminently."
  - name: "Supply chain compromise via package registries (Shai-Hulud campaign)"
    mitre_id: "T1195.002"
    description: "Campaign expanded from npm to PyPI (trojanized lightning 2.6.2/2.6.3). Self-propagating worm injecting malicious code into victims' own published packages. Harvests SSH keys, cloud credentials, and tokens."

active_campaigns:
  - actor: "CL-STA-1132 (likely state-sponsored)"
    targets: "Organizations with internet-exposed PAN-OS User-ID Authentication Portals"
    technique: "Exploiting CVE-2026-0300 buffer overflow for unauthenticated remote root on PA-Series and VM-Series firewalls. Active since April 9 — nearly a month before public disclosure. Tracked by Unit 42."
  - actor: "Unknown (multiple groups)"
    targets: "U.S. think tanks, European manufacturing, cloud service providers"
    technique: "Chaining web/VPN RCE with Copy Fail (CVE-2026-31431) for deterministic root escalation, then deploying ransomware or exfiltrating data. Exploitation confirmed increasing."
  - actor: "ShinyHunters"
    targets: "Healthcare (Medtronic — 9M records), SaaS platforms (Vimeo via Anodot)"
    technique: "Targeting third-party vendors with access to primary victim data. Exfiltrating terabytes, threatening leaks. Ransom payments suspected based on leak site removals."
  - actor: "UAT-8302 (China-nexus)"
    targets: "Government entities in South America and southeastern Europe"
    technique: "Sophisticated APT with full malware toolkit targeting sovereign infrastructure. Disclosed by Cisco Talos. Active since late 2024."
  - actor: "Shai-Hulud operators"
    targets: "npm and PyPI ecosystems, downstream consumers of compromised packages"
    technique: "Self-propagating supply chain worm. PyPI variant (lightning 2.6.2/2.6.3) harvests credentials and crypto wallets."

stack_intersection:
  - alert: "Copy Fail affects our Linux host kernel — verify Railway platform patching status. 8 days to KEV deadline."
  - alert: "CVE-2026-0300 PAN-OS zero-day — state-sponsored exploitation since April 9. Check if Palo Alto firewalls are in our network path. CISA FCEB deadline May 9 (2 days)."
  - alert: "CVE-2026-23918 Apache HTTP/2 RCE — check if Railway uses Apache in its proxy layer. Our gunicorn is not directly affected."
  - alert: "Shai-Hulud PyPI variant means our requirements.txt integrity hash gap is actively exploitable, not theoretical."
  - alert: "Bleeding Llama (CVE-2026-7482) relevant if any team members run local Ollama instances — memory leak exposes chat history and credentials."

landscape_summary: "CVE-2026-0300 remains the dominant threat — state-sponsored cluster CL-STA-1132 has been exploiting this unpatched PAN-OS firewall zero-day since April 9, nearly a month before public disclosure. CISA FCEB deadline is May 9 (2 days away), no patch until May 13. Copy Fail (CVE-2026-31431) exploitation continues expanding with 8 days to KEV deadline. New this cycle: Bleeding Llama (CVE-2026-7482) exposes 300K Ollama instances to unauthenticated memory exfiltration — AI infrastructure security is becoming an active attack surface. Apache HTTP/2 RCE (CVE-2026-23918) weaponization remains imminent. For our stack: no direct dependency CVEs, but host kernel patching, perimeter device exposure, pip integrity hashes, and local AI tool security are the closest intersection points."
