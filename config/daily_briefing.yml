# Daily Briefing — generated by Peter Parker late PM routine.
# active:false and the frontend hides the briefing block entirely.
# This file is overwritten each run; do not manually edit unless testing.
active: true
generated_at: "2026-05-28T22:00:00Z"
actions:
  - severity: "critical"
    title: "Exchange CVE-2026-42897 — CISA KEV Deadline TOMORROW (May 29 EOD): OWA Restriction Non-Negotiable, No Permanent Patch"
    description: "The CISA KEV federal remediation deadline for Exchange CVE-2026-42897 (reflected XSS in OWA via crafted email, session hijack confirmed, CVSS 8.1) expires end-of-day May 29 — 24 hours from now. No permanent patch exists; the only reliable defense is OWA restriction to VPN/zero-trust. EEMS/EOMT automatic mitigation modules have confirmed gaps discovered May 19 — any verification performed before that date is invalidated and must be redone. Active exploitation ongoing since May 14 (Microsoft-confirmed in-the-wild attacks). Exchange 2016 Period 1 ESU receives no permanent fix."
    action: "Restrict OWA to VPN/zero-trust access NOW — this is the only reliable current defense given persistent EEMS/EOMT gaps and no permanent patch. Re-verify EEMS/EOMT mitigation modules on every on-premises Exchange instance; any verification predating May 19 is invalidated. Hunt OWA access logs for crafted-email XSS delivery patterns and anomalous session activity since May 14. Exchange 2016 Period 1 ESU operators: no permanent fix is coming — restrict OWA and initiate accelerated migration. Federal agencies: formal CISA KEV deadline is May 29 EOD — document remediation or mitigation status now."

  - severity: "critical"
    title: "TrapDoor Supply Chain: CLAUDE.md and .cursorrules Poisoning Hijacks AI Coding Assistants Across npm/PyPI/Crates.io"
    description: "TrapDoor (first detected May 22 by Socket.dev) remains active across npm, PyPI, and Crates.io with 34 malicious packages and 384+ versions. The defining technique — malicious CLAUDE.md and .cursorrules files embedding zero-width Unicode characters (U+200B, U+200C, U+200D) that hide instructions tricking Claude Code, Cursor, and Copilot into exfiltrating developer credentials under cover of a fake 'security scan' — is confirmed linked to prior CWE-78 command injection flaws in AI coding assistant CLI tools. Poisoned PRs submitted to browser-use, LangChain, and LangFlow. Socket median detection: 5 minutes 27 seconds."
    action: "Audit all CLAUDE.md and .cursorrules files in active repos and open PRs for zero-width Unicode: grep -rP '[\\x{200B}\\x{200C}\\x{200D}\\x{FEFF}]' . — any match is a TrapDoor IOC. Audit all PRs opened since May 22 by external contributors touching AI config files. Inspect .git/hooks/ for unexpected scripts. Rotate AWS IAM keys, GitHub PATs, npm/PyPI publish tokens, and SSH private keys on any developer machine or CI environment that ran unverified packages since May 22. Independent of Mini Shai-Hulud and Glassworm — requires separate remediation."

  - severity: "high"
    title: "Silent Ransom Group — FBI Flash Alert: Physical Operatives Enter Law Firm Offices to Insert USB Data-Theft Devices"
    description: "The FBI issued a FLASH alert May 26 warning that Silent Ransom Group (SRG, Russia-linked extortion gang active since 2023) has escalated from remote phishing to physically sending operatives into law firm offices posing as IT support personnel. When a remote desktop session 'fails,' SRG dispatches an operative to the victim's location to physically insert a USB data-exfiltration device. No malware, no encryption, no ransomware payload — no desktop lockscreen or splash screen. The attack is invisible until a ransom email arrives threatening to post stolen data. 38+ law firms have data on SRG's public leak site; total attack count exceeds 100 with Q1 2026 surging sharply."
    action: "Implement strict visitor identity verification: require pre-arranged IT visit tickets and callback verification to known-good IT numbers before granting any claimed support personnel physical workstation access — do not trust walk-in IT staff, ever. Enforce USB port blocking or hardware device whitelisting via endpoint policy (GPO/MDM) on all employee workstations, especially reception desks and shared-access machines. Brief all staff — particularly legal, finance, and executive assistants — on the SRG social engineering script: a prior phishing email or call, then 'IT personnel' arriving to fix a remote access issue. Hunt endpoint logs for unexpected USB device insertions since January 2026."

  - severity: "high"
    title: "Gitea CVE-2026-27771 — 30,000 Deployments Exposed Private Container Images Without Authentication for ~4 Years"
    description: "CVE-2026-27771 (CVSS 8.2), disclosed May 27 by NoScope via responsible disclosure, allows any unauthenticated attacker to pull private container images from Gitea deployments running versions prior to 1.26.2 — no account, no password, no token required. The 'private' container repository designation failed to enforce authentication on registry endpoints. The flaw went undetected for approximately four years. Approximately 30,000 Gitea self-hosted deployments across 30+ countries are affected, including healthcare providers, aerospace manufacturers, and ISPs."
    action: "Upgrade all Gitea instances to 1.26.2 immediately. If immediate patching is not possible, set [service].REQUIRE_SIGNIN_VIEW=true in gitea.ini as a temporary mitigation. Audit container registry access logs for unauthenticated pulls (requests without Authorization header) going back to at least May 2022 — any match means private images were exfiltrated. Rotate all secrets, API keys, or credentials embedded in private container images if authenticated-only access cannot be confirmed from logs. Treat any private Gitea-hosted container image as potentially compromised if the instance was running < 1.26.2."

  - severity: "high"
    title: "PAN-OS CVE-2026-0300 Phase 2 Patches Complete — 49-Day CL-STA-1132 Dwell Demands Parallel Incident Response"
    description: "Palo Alto Networks released Phase 2 patches for CVE-2026-0300 today (May 28), completing coverage across all supported PAN-OS trains. CL-STA-1132 (China-nexus state actor) has exploited this CVSS 9.3 unauthenticated RCE since April 9 — Day 49. Any PAN-OS instance unpatched since April 9 must be treated as fully compromised with 49 days of potential persistent nation-state access: mapped network topology, credential exfiltration, and lateral movement are assumed at full dwell maturity."
    action: "Apply Phase 2 patches immediately: 12.1.7 / 11.2.4-h17 / 11.2.12 / 11.1.7-h6 / 11.1.15 / 10.2.7-h34 / 10.2.13-h21 / 10.2.16-h7. For any instance unpatched since April 9: initiate incident response in parallel with patching — hunt CL-STA-1132 IOCs (unauthorized SSH keys, anomalous management plane connections, unexpected NETCONF events, outbound tunneling not in baseline). Patching does not retroactively evict a persistent actor; assume ongoing access until forensically cleared."

stats:
  - label: "exchange kev deadline (days remaining)"
    value: 1
    hot: true
  - label: "silent ransom group law firms leaked"
    value: 38
    hot: true
  - label: "gitea deployments exposed (k)"
    value: 30
    hot: true
  - label: "trapdoor malicious package versions"
    value: 384
    hot: false
  - label: "pan-os cl-sta-1132 dwell days"
    value: 49
    hot: false
