# Daily Briefing — generated by Peter Parker PM routine.
# active:false and the frontend hides the briefing block entirely.
# This file is overwritten each run; do not manually edit unless testing.
active: true
generated_at: "2026-05-26T18:00:00Z"
actions:
  - severity: "critical"
    title: "Drupal CVE-2026-9082 — CISA KEV DEADLINE TOMORROW May 27 (T-1 DAY): Patch or Emergency-Block All PostgreSQL Drupal Now"
    description: "Unauthenticated SQL injection in Drupal Core PostgreSQL EntityQuery (SA-CORE-2026-004, CVSS 6.5) — CISA KEV federal deadline is TOMORROW May 27, fewer than 24 hours remaining. Attack volume exceeds 15,000+ exploitation attempts across 65 countries; the public PoC scanner (circulating since May 22) is accelerating scan rates today. Organizations still running unpatched PostgreSQL Drupal are under active exploitation right now, and the Verizon DBIR 2026 43-day median patch window means late-patching organizations are statistically past their highest-risk exposure point on this exact vector."
    action: "Patch all PostgreSQL-backed Drupal sites to 11.3.10 / 10.6.9 / 10.5.10 / 10.4.10 TONIGHT — CISA deadline expires tomorrow. If a site cannot patch before tomorrow: restrict all Drupal access to authenticated sessions only as emergency block. Confirm database backend type — MySQL/MariaDB/SQLite backends are NOT affected; any unconfirmed instance should be treated as PostgreSQL until proven otherwise. Call all Drupal-running partner orgs tonight; they have fewer than 24 hours."

  - severity: "critical"
    title: "PAN-OS CVE-2026-0300 — CL-STA-1132 (China-Nexus) Day 47, Patch Window Closes TOMORROW May 28 (T-2 DAYS)"
    description: "CL-STA-1132 (China-nexus state actor) has exploited CVE-2026-0300 (PAN-OS buffer overflow, unauthenticated remote root via User-ID Authentication Portal) continuously since April 9 — now Day 47. The full patch window closes May 28, meaning this is the penultimate day to apply patches before the defined remediation deadline expires. Any PAN-OS instance internet-exposed and unpatched since April 9 must be treated as compromised: 47 days of potential persistent access with CL-STA-1132's confirmed playbook of SSH key injection, lateral movement, and exfiltration during full dwell."
    action: "Apply PAN-OS patches for CVE-2026-0300 IMMEDIATELY — patch window closes tomorrow (May 28). Any PAN-OS unpatched since April 9: initiate incident response, hunt for CL-STA-1132 IOCs (SSH keys, unexpected management connections, NETCONF events), and assume lateral movement has occurred. Restrict User-ID Authentication Portal to trusted zones only as interim control for instances that cannot patch today. This is the most urgent infrastructure-level deadline expiring in the next 48 hours."

  - severity: "critical"
    title: "Exchange CVE-2026-42897 — CISA KEV Deadline May 29 (T-3 Days), Confirmed Mitigation Gaps Since May 19"
    description: "Actively exploited reflected XSS in Exchange OWA executes arbitrary JavaScript in victim browser via crafted email — session hijack confirmed. CISA KEV deadline May 29 (T-3 days). No permanent patch exists; EEMS/EOMT mitigation modules have confirmed gaps discovered May 19, meaning previously verified-mitigated instances may still be exploitable. Exchange 2016 Period 1 ESU customers will receive no permanent fix — permanent patch expected only for Exchange SE RTM, 2016 CU23, 2019 CU14/CU15 with no committed date."
    action: "Re-verify EEMS/EOMT mitigation modules are active and correctly configured on every on-premises Exchange instance — confirmed gaps mean prior mitigation status cannot be trusted without re-checking after May 19. Restrict OWA to VPN/zero-trust access: most reliable current defense given confirmed gaps. Hunt OWA access logs for crafted-email XSS delivery patterns. Exchange 2016 Period 1 ESU operators: no permanent fix is coming — restrict OWA to VPN immediately and begin accelerated migration planning."

  - severity: "high"
    title: "Ghost CMS CVE-2026-26980 — 700+ Sites Hijacked for ClickFix Campaign; Harvard, Oxford, DuckDuckGo Compromised"
    description: "Unauthenticated blind SQL injection in Ghost CMS Content API (CVE-2026-26980, CVSS 9.4) — patched February 2026 in Ghost 6.19.1 — is being exploited at scale today to steal admin API keys, inject obfuscated JavaScript into all published articles, and redirect visitors to fake Cloudflare CAPTCHA ClickFix pages that execute credential-stealing malware. Over 700 domains are confirmed compromised including Harvard University, Oxford University, Auburn University, DuckDuckGo, and multiple AI/SaaS/fintech/security-research sites. Two competing threat actor groups are simultaneously operating this campaign, with some sites being re-compromised within the same day."
    action: "If running Ghost CMS: update to 6.19.1+ immediately (patch available since February 2026; any Ghost < 6.19.1 on a PostgreSQL or MySQL backend is vulnerable). Audit all Ghost content for injected JavaScript (search article bodies for base64-encoded scripts, unexpected <script> tags, dynamic script loaders). Review Ghost Admin API access logs for unauthorized key usage since May 7. Rotate Admin API keys. If already compromised: remove all injected scripts, notify users who may have been served ClickFix pages, and treat those sessions as potentially serving credential-stealing malware to visitors."

  - severity: "high"
    title: "Mini Shai-Hulud Day 4 — TeamPCP Toolkit Day 14 in Public Circulation, Copycat Campaigns Fully Operational"
    description: "The Mini Shai-Hulud supply chain worm (CVE-2026-45321) enters Day 4 with campaign totals sustained at 1,055 compromised package versions across 502 packages; 25,000+ GitHub repos credential-leaked, 2,500+ malicious repos created. The full worm toolkit — CI cache-poisoning scripts, OIDC token extractor, credential stealer with self-propagation logic — has been publicly circulating for 14 days (GitHub May 12, BreachForums May 24 with active $1,000 copycat competition). Copycat actors are fully operational; the risk is no longer limited to original TeamPCP and is now multi-actor and unsolicited. The Verizon DBIR 2026 confirms supply chain is now present in 48% of all corporate breaches."
    action: "Implement pip install --require-hashes via pip-tools BEFORE next deployment — single highest-leverage unresolved defensive action after 14 days of public toolkit. Audit all CI/CD pipelines for package installs since May 11 against the 502-package poisoned list. Run git log .github/workflows/ since May 18 for commits authored by build-bot, auto-ci, ci-bot, or pipeline-bot (Megalodon Day 8 IOCs). Rotate all GitHub OIDC tokens and npm/PyPI publish tokens. Restrict id-token: write permissions to minimum-scope workflows."

stats:
  - label: "drupal kev deadline (days remaining)"
    value: 1
    hot: true
  - label: "panos patch window (days remaining)"
    value: 2
    hot: true
  - label: "exchange kev deadline (days remaining)"
    value: 3
    hot: true
  - label: "ghost cms sites hijacked for clickfix"
    value: 700
    hot: true
  - label: "mini shai-hulud compromised packages"
    value: 502
    hot: false
