# Daily Briefing — generated by Peter Parker AM routine.
# active:false and the frontend hides the briefing block entirely.
# This file is overwritten each run; do not manually edit unless testing.
active: true
generated_at: "2026-05-29T07:00:00Z"
actions:
  - severity: "critical"
    title: "Exchange CVE-2026-42897 — CISA KEV Deadline EXPIRES TODAY (May 29 EOD): OWA Restriction Is the Only Reliable Defense"
    description: "The CISA KEV federal remediation deadline for Exchange CVE-2026-42897 (reflected XSS via crafted email enabling OWA browser session hijack, CVSS 8.1) expires today end-of-day May 29 — active exploitation has continued since May 14 (Microsoft-confirmed in-the-wild attacks), no permanent patch exists, and EEMS/EOMT mitigation gaps confirmed on May 19 mean any verification performed before that date is invalidated and must be re-run."
    action: "Restrict OWA to VPN/zero-trust access if not already done — this is the only reliable current defense given no permanent patch and persistent EEMS/EOMT gaps. Re-verify EEMS/EOMT mitigation modules on every on-premises Exchange instance; only post-May-19 verifications count. Hunt OWA access logs for crafted-email XSS delivery patterns and anomalous session activity since May 14. Exchange 2016 Period 1 ESU operators: no permanent fix is coming — restrict OWA now and begin accelerated migration. Federal agencies: document remediation or mitigation status for CISA KEV compliance today."

  - severity: "critical"
    title: "TrapDoor Supply Chain + AI Coding Assistant Poisoning — Campaign Active Day 10, 384+ Malicious Versions Across npm/PyPI/Crates.io"
    description: "TrapDoor (active since May 19, first detected May 22 by Socket.dev) continues publishing credential-stealing packages across npm, PyPI, and Crates.io — 34 distinct packages, 384+ versions, no takedown — and targets Claude Code, Cursor, and Copilot via CLAUDE.md and .cursorrules files laced with zero-width Unicode characters (U+200B/200C/200D) that embed hidden instructions triggering a fake 'security scan' which silently exfiltrates AWS IAM keys, GitHub PATs, SSH private keys, and crypto wallet keystores."
    action: "Audit all CLAUDE.md and .cursorrules files for zero-width Unicode: grep -rP '[\\x{200B}\\x{200C}\\x{200D}\\x{FEFF}]' . — any match is a TrapDoor IOC. Inspect .git/hooks/ for unexpected scripts. Audit all PRs opened since May 19 by external contributors touching AI config files. Rotate AWS IAM keys, GitHub PATs, npm/PyPI publish tokens, and SSH private keys on any developer machine or CI/CD environment that ran unverified packages since May 19. Independent of SymJack and Mini Shai-Hulud — requires separate remediation."

  - severity: "high"
    title: "SymJack — Malicious Repository Symlinks Silently Install Attacker-Controlled MCP Servers via AI Coding Agent Trust"
    description: "Adversa AI research (SecurityWeek) disclosed that malicious repositories can embed renamed symlinks disguised as ordinary files that trick AI coding agents into inserting attacker-controlled MCP servers into their own configuration during an approved file-copy operation — once installed, the rogue MCP server can steal SSH keys, cloud tokens, and browser sessions, compromise CI pipelines, and deploy malicious code without any further user interaction; confirmed against Claude Code, Cursor, Copilot, Gemini CLI, Grok Build, and OpenAI Codex CLI."
    action: "Update Claude Code to the latest hardened build — updated versions resolve symlinks before presenting the approval prompt and display real destination paths, eliminating the deception. Audit ~/.claude/mcp_servers and any .mcp.json files for unexpected server entries added since you last cloned an external repository. Treat any unrecognized MCP server configuration as a potential SymJack IOC and rotate credentials on any machine that may have run a compromised agent session. Never approve AI agent file operations from untrusted or unfamiliar repositories without manually inspecting symlink destinations."

  - severity: "high"
    title: "CVE-2026-41089 — Windows Netlogon Wormable CVSS 9.8: May Patch Tuesday Fix 17 Days Old, Unpatched DCs Remotely Exploitable"
    description: "CVE-2026-41089 (CVSS 9.8, disclosed May 12 Patch Tuesday) is a stack-based buffer overflow in Windows Netlogon's BuildSamLogonResponse handler allowing any unauthenticated network attacker reachable on Netlogon RPC (TCP 135/445) to execute code as SYSTEM on domain controllers with no user interaction — classified wormable, capable of autonomous propagation from DC to DC in the same class as MS08-067 (Conficker) and Zerologon (CVE-2020-1472); affects all Windows Server releases 2012 through 2025."
    action: "Verify the May 2026 cumulative update (KB5087539 for Server 2025; equivalent KB for each Server version) is applied on every domain controller — this is the highest-priority Windows infrastructure patch this cycle. Block external access to Netlogon RPC (TCP 135, 445) at the perimeter firewall on all domain controllers. Segment domain controllers onto isolated VLANs inaccessible from general user network segments. Audit Windows Event Log for unexpected Netlogon authentication bursts or mass authentication failures that may indicate active scanning or exploitation attempts."

  - severity: "high"
    title: "FortiClient CVE-2026-35616 — EKZ Infostealer Delivered as Fake Fortinet Security Update, CISA KEV Day 53"
    description: "CVE-2026-35616 (CVSS 9.1, CISA KEV listed April 6 — Day 53 of active exploitation), a pre-authentication API access bypass in FortiClient EMS, has been weaponized to push the EKZ infostealer to all managed endpoints disguised as a legitimate Fortinet update via the VPN scripting workflow — EKZ silently extracts stored credentials, credit card data, and session cookies from Chromium and Firefox browsers before removing its artifacts, confirmed by Arctic Wolf across multiple victim organizations in May 2026."
    action: "Upgrade FortiClient EMS to version 7.4.7 or later immediately — 7.4.7 is the definitive fix (7.4.5 hotfix and 7.4.6 are emergency interim patches). Audit EMS scripting logs for unauthorized PowerShell invocations or unexpected script-push events. Hunt endpoints for sqlite3 read events against Chrome or Firefox profile directories co-occurring with FortiClient process execution — these are EKZ exfiltration IOCs. Rotate all browser-stored credentials on any endpoint managed by a FortiClient EMS instance that was not patched before May 2026."

stats:
  - label: "exchange kev deadline (days remaining)"
    value: 0
    hot: true
  - label: "trapdoor malicious package versions"
    value: 384
    hot: true
  - label: "netlogon wormable days since patch"
    value: 17
    hot: true
  - label: "forticlient ekz kev days elapsed"
    value: 53
    hot: true
  - label: "pan-os cl-sta-1132 dwell days"
    value: 50
    hot: false
