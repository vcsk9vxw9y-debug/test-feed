# Daily Briefing — generated by morning briefing scheduled task.
# active:false and the frontend hides the briefing block entirely.
# This file is overwritten daily; do not manually edit unless testing.
active: true
generated_at: "2026-05-04T12:00:00Z"
actions:
  - severity: "critical"
    title: "CVE-2026-31431 — Copy Fail Linux root exploit, CISA KEV deadline May 15"
    description: "CISA confirmed active exploitation of this nine-year-old Linux kernel flaw. Attackers chaining it with web/VPN RCE bugs for ransomware deployment. Deterministic root via 732-byte script on every distro since 2017. Confirmed attacks on think tanks, manufacturing, cloud providers."
    action: "Apply kernel patches from your distro vendor (Ubuntu, RHEL, SUSE, Amazon Linux all have fixes), reboot to load the new kernel, prioritize internet-facing Linux hosts, audit system logs for unauthorized privilege escalation since late April, update container host kernels"
  - severity: "critical"
    title: "CVE-2026-41940 — cPanel 'Sorry' ransomware wave, KEV deadline passed"
    description: "CISA KEV deadline passed May 3. The cPanel auth bypass is now being mass-exploited in 'Sorry' ransomware attacks encrypting hosted sites. 44K+ IPs still scanning, 650K instances exposed, 1.5M potentially affected. Unpatched servers should be assumed compromised."
    action: "Run '/scripts/upcp --force' immediately if not already patched, verify with '/usr/local/cpanel/cpanel -V', block ports 2083/2087/2095/2096, audit WHM access logs back to February 23 for unauthorized root sessions, check for 'Sorry' ransomware indicators"
  - severity: "high"
    title: "CVE-2026-32202 + CVE-2024-1708 — State-sponsored exploitation of Windows Shell and ScreenConnect"
    description: "APT28 (Russia) exploiting incomplete Windows Shell patch, Kimsuky (North Korea) deploying ToddlerShark malware via ScreenConnect path traversal. CISA KEV deadline May 12."
    action: "Apply May cumulative Windows updates, patch ConnectWise ScreenConnect to latest version, hunt for ToddlerShark indicators (scheduled task persistence, registry defense lowering), restrict ScreenConnect server exposure"
  - severity: "high"
    title: "PyTorch Lightning supply chain — Shai-Hulud campaign still active"
    description: "Trojanized 'lightning' PyPI versions 2.6.2/2.6.3 harvest SSH keys, cloud credentials, GitHub/npm tokens. Part of the broader Shai-Hulud supply chain campaign that began with the Bitwarden CLI npm worm."
    action: "Pin lightning to version 2.6.1 or earlier, audit pip install logs for 2.6.2/2.6.3, rotate SSH keys and cloud credentials on affected machines, scan for hidden _runtime directories, add --require-hashes to pip install"
stats:
  - label: "days to copy fail kev deadline"
    value: 11
    hot: true
  - label: "linux distros affected (years)"
    value: 9
    hot: true
  - label: "cpanel servers still exposed"
    value: 650000
    hot: true
  - label: "state-sponsored kev deadline (days)"
    value: 8
    hot: false
  - label: "pypi malicious downloads (est)"
    value: 14500
    hot: false
