# Daily Briefing — generated by Peter Parker PM routine.
# active:false and the frontend hides the briefing block entirely.
# This file is overwritten each run; do not manually edit unless testing.
active: true
generated_at: "2026-05-28T18:00:00Z"
actions:
  - severity: "critical"
    title: "Exchange CVE-2026-42897 — CISA KEV Deadline TOMORROW (May 29): OWA Restriction to VPN/Zero-Trust Non-Negotiable"
    description: "The CISA KEV federal remediation deadline for Exchange CVE-2026-42897 (reflected XSS in OWA via crafted email, session hijack confirmed, CVSS 8.1) expires end-of-day May 29 — tomorrow. No permanent patch exists. EEMS/EOMT mitigation modules have confirmed gaps discovered May 19; any verification performed before May 19 is invalid and must be redone. Exchange 2016 Period 1 ESU receives no permanent fix. Active exploitation ongoing since May 14 with Microsoft-confirmed in-the-wild attacks."
    action: "Restrict OWA to VPN/zero-trust access NOW — this is the only reliable current defense given persistent EEMS/EOMT gaps and no permanent patch. Re-verify EEMS/EOMT mitigation modules on every on-premises Exchange instance (any verification predating May 19 is invalidated). Hunt OWA access logs for crafted-email XSS delivery patterns and anomalous session activity since May 14. Exchange 2016 Period 1 ESU operators: no permanent fix is coming — restrict OWA to VPN/zero-trust and initiate accelerated migration. Federal agencies: CISA compliance documentation must be completed by end of business today — deadline is tomorrow EOD, federal reporting window closes tonight."

  - severity: "critical"
    title: "PAN-OS CVE-2026-0300 Phase 2 Patches Released Today — Apply Immediately, CL-STA-1132 Day 49"
    description: "Palo Alto Networks released Phase 2 patches for CVE-2026-0300 today (May 28), completing coverage across all supported PAN-OS trains. This closes the CVSS 9.3 unauthenticated RCE vulnerability exploited by CL-STA-1132 (China-nexus state actor) since April 9 — Day 49 of continuous exploitation. Any PAN-OS instance unpatched since April 9 must be treated as fully compromised with 49 days of potential persistent nation-state access: mapped network topology, credential exfiltration, and lateral movement are assumed."
    action: "Apply Phase 2 patches NOW: 12.1.7 / 11.2.4-h17 / 11.2.12 / 11.1.7-h6 / 11.1.15 / 10.2.7-h34 / 10.2.13-h21 / 10.2.16-h7 — verify your version train and apply the correct build. For any instance unpatched since April 9: initiate incident response in parallel with patching — hunt CL-STA-1132 IOCs (unauthorized SSH keys, anomalous management plane connections, unexpected NETCONF events, outbound tunneling not in baseline). Phase 2 does not retroactively clean a compromised host. Restrict User-ID Authentication Portal to trusted management zones on any instance still awaiting patching."

  - severity: "critical"
    title: "TrapDoor Supply Chain: CLAUDE.md and .cursorrules Poisoning Hijacks AI Coding Assistants Across npm/PyPI/Crates.io"
    description: "TrapDoor (first detected May 22 by Socket.dev) remains active across npm, PyPI, and Crates.io with 34 malicious packages and 384+ versions. The campaign's defining technique — malicious CLAUDE.md and .cursorrules files with zero-width Unicode characters (U+200B, U+200C, U+200D) hiding instructions that trick Claude Code, Cursor, and Copilot into exfiltrating developer credentials under cover of a fake 'security scan' — is confirmed linked to prior CWE-78 command injection flaws in AI coding assistant CLI tools. Poisoned PRs submitted to browser-use, LangChain, and LangFlow. Socket detection median: 5 minutes 27 seconds."
    action: "Audit all CLAUDE.md and .cursorrules files in active repos and open PRs for zero-width Unicode: grep -rP '[\\x{200B}\\x{200C}\\x{200D}\\x{FEFF}]' . — any match is a TrapDoor IOC. Audit all PRs opened since May 22 by external contributors touching AI config files. Inspect .git/hooks/ for unexpected scripts. Rotate AWS IAM keys, GitHub PATs, npm/PyPI publish tokens, and SSH private keys on any developer machine or CI environment that ran unverified PyPI/npm/Crates packages since May 22. This is independent of Mini Shai-Hulud and Glassworm — all three require separate remediation."

  - severity: "high"
    title: "BadHost CVE-2026-48710 — Starlette Host-Header Auth Bypass in AI Infrastructure (325M Weekly Downloads)"
    description: "BadHost (CVE-2026-48710, disclosed May 22 by X41 D-Sec via OSTIF audit) is a host-header authentication bypass in Starlette < 1.0.1 — the Python ASGI framework underlying FastAPI, vLLM, LiteLLM, and MCP servers. Starlette rebuilds URLs from the Host header without validating it, allowing attackers to inject unauthenticated paths and bypass path-based auth middleware with a single malformed header. MCP servers are especially exposed: they expose unauthenticated OAuth discovery endpoints by design, giving attackers a predictable entry point. 325 million weekly downloads make this one of the broadest-surface Python auth vulnerabilities disclosed this year."
    action: "Patch to Starlette >= 1.0.1 immediately on all FastAPI, vLLM, LiteLLM, and MCP deployments. Use the free scanner at badhost.org or the open-source Semgrep/CodeQL rules from the X41 repository to confirm exposure before patching. Audit all AI inference servers, LLM API gateways, and agent framework endpoints for external exposure. For MCP servers specifically: treat all OAuth discovery endpoints as externally reachable by default and verify authentication enforcement after patching. Starlette is not in our requirements.txt — no action on the Threat-Feed app itself, but audit any adjacent tooling or developer MCP servers."

  - severity: "medium"
    title: "Drupal CVE-2026-9082 — Federal KEV Non-Compliance Day 1, 670+ Sites Still Actively Scanned"
    description: "The CISA KEV federal remediation deadline for Drupal CVE-2026-9082 (unauthenticated SQL injection in PostgreSQL EntityQuery, CVSS 6.5) expired yesterday May 27. Day 1 of federal non-compliance. Shadowserver continues tracking approximately 670+ unpatched internet-exposed Drupal installations; the public PoC scanner (circulating since May 22) is actively targeting each remaining site. Deadline expiry does not reduce exploitability."
    action: "Patch all PostgreSQL-backed Drupal sites to 11.3.10 / 10.6.9 / 10.5.10 / 10.4.10 (or 11.2.12 / 11.1.10) immediately. Confirm database backend — MySQL, MariaDB, and SQLite are NOT affected; treat any unverified instance as PostgreSQL until confirmed. Federal agencies: document remediation or mitigation status for CISA non-compliance reporting. Escalate to all partner organizations running PostgreSQL-backed Drupal."

stats:
  - label: "exchange kev deadline (days remaining)"
    value: 1
    hot: true
  - label: "pan-os cl-sta-1132 dwell days"
    value: 49
    hot: true
  - label: "badhost starlette weekly downloads (M)"
    value: 325
    hot: true
  - label: "trapdoor malicious package versions"
    value: 384
    hot: false
  - label: "drupal kev days past deadline"
    value: 1
    hot: false
