# Daily Briefing — generated by Peter Parker AM routine.
# active:false and the frontend hides the briefing block entirely.
# This file is overwritten each run; do not manually edit unless testing.
active: true
generated_at: "2026-05-28T06:00:00Z"
actions:
  - severity: "critical"
    title: "PAN-OS CVE-2026-0300 Phase 2 Patches RELEASED TODAY — Apply NOW, CL-STA-1132 Day 49"
    description: "Palo Alto Networks released Phase 2 patches for CVE-2026-0300 TODAY (May 28) completing coverage across PAN-OS 10.2, 11.1, 11.2, and 12.1 — all supported versions now have fixed releases available. This closes the CVSS 9.3 unauthenticated RCE vulnerability exploited by CL-STA-1132 (China-nexus state actor) since April 9, now Day 49 of continuous exploitation. Phase 2 patches do NOT retroactively remediate a compromised host — any PAN-OS instance unpatched since April 9 must be treated as fully compromised with 49 days of potential persistent nation-state access and credential exfiltration, requiring simultaneous IR alongside patching."
    action: "Apply Phase 2 patches NOW: 12.1.7 / 11.2.4-h17 / 11.2.12 / 11.1.7-h6 / 11.1.15 / 10.2.7-h34 / 10.2.13-h21 / 10.2.16-h7 — verify your version train and apply the correct build. For any instance unpatched since April 9: initiate incident response in parallel with patching — hunt CL-STA-1132 IOCs (unauthorized SSH keys, anomalous management plane connections, unexpected NETCONF events, outbound tunneling not in your baseline). Assume persistent access, mapped network topology, and credential exfiltration have occurred during the 49-day dwell. Restrict User-ID Authentication Portal to trusted management zones on instances still awaiting patching."

  - severity: "critical"
    title: "TrapDoor Supply Chain: CLAUDE.md and .cursorrules Poisoning Hijacks AI Assistants Across npm/PyPI/Crates.io"
    description: "A new coordinated supply chain campaign (TrapDoor, first detected May 22 by Socket.dev) is actively targeting npm, PyPI, and Crates.io with 34 malicious packages and 384+ versions, introducing an unprecedented technique: malicious .cursorrules and CLAUDE.md files laced with zero-width Unicode characters (U+200B, U+200C, U+200D) contain hidden instructions that trick AI coding assistants (Claude Code, Cursor, Copilot) into running a fake 'security scan' that silently exfiltrates developer credentials. TrapDoor also plants persistence via Git hooks, shell hooks, systemd, cron, and SSH key injection. Validated stolen AWS and GitHub credentials via API calls to prioritize high-value secrets. The malicious packages were submitted via pull requests to prominent AI projects including LangChain, MetaGPT, and OpenHands."
    action: "Audit all CLAUDE.md and .cursorrules files in active projects and open PRs for zero-width Unicode characters: grep -rP '[\\x{200B}\\x{200C}\\x{200D}\\x{FEFF}]' . — any match is a TrapDoor IOC. Review all PRs opened since May 22 by unknown contributors to repositories hosting AI configuration files. Inspect Git hooks directory (.git/hooks/) for unexpected scripts added since May 22. Verify all packages installed from PyPI, npm, and Crates.io since May 22 against the TrapDoor IOC list (Socket.dev advisory). Rotate AWS IAM keys, GitHub PATs, npm/PyPI publish tokens, and SSH private keys on any developer machine or CI environment that ran affected packages without hash verification. This is separate from Mini Shai-Hulud and Glassworm — all three developer-targeting campaigns require independent remediation."

  - severity: "critical"
    title: "Exchange CVE-2026-42897 — CISA KEV Deadline TOMORROW (May 29): Restrict OWA to VPN/Zero-Trust Today"
    description: "The CISA KEV federal remediation deadline for Exchange CVE-2026-42897 (reflected XSS in OWA via crafted email, session hijack confirmed, CVSS 8.1) expires TOMORROW end-of-day May 29. No permanent patch exists. EEMS/EOMT mitigation modules have confirmed gaps discovered May 19 — any verification performed before May 19 is invalid and must be redone. Exchange 2016 Period 1 ESU receives no permanent fix. Active exploitation ongoing since May 14 with Microsoft-confirmed in-the-wild attacks; deadline falls within the 43-day median patch window (Verizon DBIR 2026), meaning most organizations remain statistically exposed."
    action: "Restrict OWA to VPN/zero-trust access TODAY — this is the most reliable current defense given persistent EEMS/EOMT gaps and the absence of a permanent patch. Re-verify EEMS/EOMT mitigation modules on every on-premises Exchange instance; any verification predating May 19 is invalidated and must be redone. Hunt OWA access logs for crafted-email XSS delivery patterns and anomalous session activity since May 14 (initial exploitation date). Exchange 2016 Period 1 ESU operators: no permanent fix is coming — restrict OWA to VPN/zero-trust now and initiate accelerated migration. Federal agencies must document remediation or mitigation status for CISA by end of business today."

  - severity: "high"
    title: "Mini Shai-Hulud SLSA Provenance Bypass — First npm Worm with Valid Build Level 3 Attestations"
    description: "TeamPCP's Mini Shai-Hulud worm (Day 6 AM) has introduced a critical supply chain trust escalation: malicious npm packages carrying valid SLSA Build Level 3 provenance attestations, produced using stolen OIDC tokens with the legitimate Sigstore stack. This is the first documented case of a supply chain worm bypassing SLSA integrity verification. SLSA Build Level 3 attestation confirms which CI pipeline produced an artifact — it does not validate whether that pipeline was behaving as intended. Organizations relying on SLSA provenance as a trust signal for packages installed since May 12 must reassess that trust model. Campaign totals: 1,055 versions / 502 packages / 25,000+ repos credential-leaked; BreachForums copycat competition Day 5 active."
    action: "Do not treat SLSA Build Level 3 attestations alone as sufficient trust verification for npm packages acquired since May 12 — stolen OIDC tokens allow attackers to produce cryptographically valid attestations for backdoored packages. Continue pip install --require-hashes for Python dependencies (pinning to pre-compromise known-good hashes remains the best available defense for our PyPI stack). Audit CI/CD pipeline OIDC token configurations: revoke any token that previously published to npm or PyPI and reissue with minimum-scope id-token: write permissions restricted to specific workflow jobs. Review SLSA attestation issuers for pinned packages; any attestation originating from a pipeline that executed Mini Shai-Hulud-era package installs should be treated as potentially contaminated."

  - severity: "medium"
    title: "Drupal CVE-2026-9082 — Federal Non-Compliance Day 1: 670+ Sites Still Actively Scanned"
    description: "The CISA KEV federal remediation deadline for Drupal CVE-2026-9082 (unauthenticated SQL injection in PostgreSQL EntityQuery, CVSS 6.5) expired yesterday May 27. Shadowserver continues tracking approximately 670+ unpatched internet-exposed Drupal installations; the public PoC scanner (circulating since May 22) is actively targeting each remaining site. Federal agencies not yet patched are now in breach of mandatory CISA remediation orders and must document compliance status. Deadline expiry does not reduce exploitability — the vulnerability remains fully open."
    action: "Patch all PostgreSQL-backed Drupal sites to 11.3.10 / 10.6.9 / 10.5.10 / 10.4.10 (or 11.2.12 / 11.1.10) immediately. Confirm database backend — MySQL, MariaDB, and SQLite backends are NOT affected; treat any unverified instance as PostgreSQL until backend is confirmed. Federal agencies: document remediation or mitigation status for CISA reporting immediately. Escalate to all partner organizations running PostgreSQL-backed Drupal — they are in the same non-compliance window."

stats:
  - label: "pan-os cve-2026-0300 dwell days (cl-sta-1132)"
    value: 49
    hot: true
  - label: "exchange kev deadline (days remaining)"
    value: 1
    hot: true
  - label: "trapdoor malicious package versions"
    value: 384
    hot: true
  - label: "mini shai-hulud compromised packages"
    value: 502
    hot: false
  - label: "drupal kev days past deadline"
    value: 1
    hot: true
